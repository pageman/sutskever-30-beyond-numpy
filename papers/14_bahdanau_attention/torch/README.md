# PyTorch

`impl.py` is the main practical reference for this paper.

It implements a single explicit decoder step so the alignment mechanism remains the center of attention rather than the training harness.
