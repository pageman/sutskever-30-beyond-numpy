# Cubical Agda

`RelationalReasoning.agda` is intentionally thin.

What it captures:

- there is an object space and a relation space
- pairwise composition maps two objects to a relation feature
- a classifier consumes aggregated relation features

What it does not try to do:

- formalize neural nonlinearities
- prove scene-level reasoning guarantees
- encode optimization
