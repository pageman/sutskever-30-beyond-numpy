# PyTorch

`impl.py` is the main executable version of the tiny CNN classifier:

- same-padding `3x3` convolution
- ReLU
- global mean pooling
- linear classifier

It is deliberately small enough to mirror exactly in JAX.
