# tinygrad

`impl.py` is the compact CNN backend for this paper.

This is one of the strongest fits for tinygrad in the repo because the architecture remains small and the tensor program is still easy to read.
