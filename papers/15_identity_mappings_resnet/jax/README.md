# JAX

`impl.py` expresses the block as a pure pre-activation residual function.

That makes it a useful cross-check against the imperative PyTorch version while preserving the same clean skip path semantics.
