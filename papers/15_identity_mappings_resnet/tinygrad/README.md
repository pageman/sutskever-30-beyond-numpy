# tinygrad

`impl.py` is a real pre-activation residual network in tinygrad.

This is one of the better uses of tinygrad in the repo because the identity path and residual path remain very easy to inspect.
