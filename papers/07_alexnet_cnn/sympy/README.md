# SymPy

This layer is intentionally lighter than the executable backends.

- `derivations.py` encodes a scalar affine-plus-ReLU composition to represent stacked feature extraction.
- The symbolic value here is showing how piecewise-linear feature maps compose, not reproducing full convolution algebra.
