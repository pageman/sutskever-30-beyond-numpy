# PyTorch

`impl.py` is the primary practical reference for this paper.

It uses a deliberately compressed AlexNet-like stack so the model stays readable while still exercising real convolution, activation, pooling, and classification behavior.
