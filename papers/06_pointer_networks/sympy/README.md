# SymPy

Symbolic work is present here to capture the additive attention score and the log-softmax objective. That is enough to justify the gradient shape and the pointer interpretation without pretending a full symbolic treatment of sequence decoding is especially illuminating.
