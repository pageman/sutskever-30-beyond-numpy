# SymPy

The symbolic layer is intentionally focused on the residual algebra, not on full convolution identities.

- `derivations.py` encodes `f(x) = x + g(x)` and differentiates it.
- The useful point is that the derivative contains an explicit identity term.
