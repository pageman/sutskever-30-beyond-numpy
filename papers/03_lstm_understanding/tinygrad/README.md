# tinygrad

`impl.py` makes the LSTM equations executable in a compact autodiff backend.

This is exactly the kind of paper where tinygrad adds real value rather than serving as a ceremonial extra layer.
