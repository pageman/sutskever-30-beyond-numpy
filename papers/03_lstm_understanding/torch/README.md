# PyTorch

`impl.py` is the main executable implementation.

The model is a manually parameterized LSTM rather than `torch.nn.LSTM`, so the gate equations stay visible and comparable to JAX and SymPy.
